/**
 * Today — the work queue, and the home screen.
 *
 * Built in the next chunk. It is deliberately the first screen behind the gate
 * rather than a landing page, because *Today is a work queue, and it is the
 * home screen* *(R9a)*.
 */
export default function Today() {
	return (
		<main>
			<h1>Today</h1>
			<p>Nothing waiting on you.</p>
		</main>
	);
}
